@tool
class_name ActivationBehaviour
extends TimelineBehaviour

## Unity ActivationTrack ClipBehaviour analog: whether the bound object should be
## active (visible) while the playhead is inside the clip.

@export var active: bool = true
